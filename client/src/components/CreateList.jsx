// const CreateList = (props) => {

//   let button

//   if (props.title && props.description && props.phrases) {
//     button = <button onClick={props.incrementPage}>Add</button>
//   } else {
//     button = <button onClick={props.incrementPage} disabled>Add</button>
//   }

//   return (
//     <div></div>
//   )

// }

// export default CreateList